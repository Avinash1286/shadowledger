[package]
name = "shadowledger_registry"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.20.0"

[dev-dependencies]
snforge_std = "0.63.0"
assert_macros = "2.20.0"

[[target.starknet-contract]]
sierra = true

[scripts]
test = "snforge test"

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]

[tool.snforge]
fuzzer_runs = 64
fuzzer_seed = 20260819
